import { Suspense } from "react";
import { PassportCollectionScreen } from "../../features/passport/ui/passport-screens";

export default function PassportsPage() { return <Suspense><PassportCollectionScreen /></Suspense>; }
