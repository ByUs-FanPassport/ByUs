import{render,screen,fireEvent,waitFor}from"@testing-library/react";import{describe,expect,it,vi}from"vitest";import{NotificationMonitor}from"./notification-monitor";const id="11111111-1111-4111-8111-111111111111",getAccessToken=vi.fn(async()=>"token");vi.mock("@privy-io/react-auth",()=>({usePrivy:()=>({getAccessToken})}));vi.mock("next/navigation",()=>({usePathname:()=>"/admin/notifications",useSearchParams:()=>new URLSearchParams(),useRouter:()=>({replace:vi.fn()})}));let role:"viewer"|"operator"="operator";vi.mock("./use-admin-session",()=>({useAdminSession:()=>({status:"authorized",admin:{role}})}));
const data={counts:{pending:0,processing:0,sent:0,failed:1},items:[{id,channel:"kakao",kind:"benefit_won",status:"failed",attemptCount:2,nextAttemptAt:"2026-09-04T00:00:00Z",destinationLabel:"Kakao ••••1234",errorCode:"INVALID_RECIPIENT",createdAt:"2026-09-04T00:00:00Z",sentAt:null,manuallyRetryable:true}]};describe("notification monitor",()=>{it("shows only redacted delivery fields and retries for Operator",async()=>{role="operator";vi.stubGlobal("fetch",vi.fn(async(_i,init)=>init?.method==="POST"?Response.json({delivery:{}} ,{status:202}):Response.json(data)));render(<NotificationMonitor/>);expect(await screen.findByText("Kakao ••••1234")).toBeInTheDocument();expect(screen.queryByText(/@example/)).not.toBeInTheDocument();fireEvent.click(screen.getByRole("button",{name:`재시도 ${id}`}));await waitFor(()=>expect(fetch).toHaveBeenCalledWith(expect.stringContaining("/retry"),expect.objectContaining({method:"POST"}))) });it("keeps retry disabled for Viewer",async()=>{role="viewer";vi.stubGlobal("fetch",vi.fn(async()=>Response.json(data)));render(<NotificationMonitor/>);expect(await screen.findByRole("button",{name:`재시도 ${id}`})).toBeDisabled()})});
