/** Compatibility export: all creator photo registration lives in one configuration. */
export { creatorHeroImages } from "./creator-image-config";
export type { CreatorHeroImage } from "./creator-image-config";
