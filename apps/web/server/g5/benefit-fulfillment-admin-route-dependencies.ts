import "server-only";
import {createPrivyNodeAccessVerifier} from "../auth/privy-node-verifier";import {authorizeAdminSession} from "../admin/admin-session-gate";import {createSupabaseAdminSessionRepository} from "../admin/supabase-admin-session-repository";import {loadServerEnv} from "../config/env";import {createSupabaseBenefitFulfillmentAdminRepository} from "./benefit-fulfillment-admin-repository";
export function createBenefitFulfillmentAdminRouteDependencies(){const e=loadServerEnv(),verifier=createPrivyNodeAccessVerifier({appId:e.PRIVY_APP_ID,appSecret:e.PRIVY_APP_SECRET, appEnvironment: e.PRIVY_APP_ENVIRONMENT, testAccountLoginEnabled: e.PRIVY_TEST_ACCOUNT_LOGIN_ENABLED, appleLoginEnabled: e.PRIVY_APPLE_LOGIN_ENABLED}),sessions=createSupabaseAdminSessionRepository({url:e.SUPABASE_URL,serviceRoleKey:e.SUPABASE_SERVICE_ROLE_KEY});return{repository:createSupabaseBenefitFulfillmentAdminRepository({url:e.SUPABASE_URL,serviceRoleKey:e.SUPABASE_SERVICE_ROLE_KEY}),authorize:({authorization,correlationId}:{authorization:string;correlationId:string})=>authorizeAdminSession({authorization,correlationId,verifier,repository:sessions})};}
