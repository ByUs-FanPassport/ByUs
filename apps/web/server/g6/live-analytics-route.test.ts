import{describe,expect,it,vi}from"vitest";vi.mock("server-only",()=>({}));import{createGetLiveAnalyticsHandler}from"./live-analytics-route";const id="11111111-1111-4111-8111-111111111111",url="http://localhost/api/admin/analytics/live-events/x?from=2026-09-01T00%3A00%3A00.000Z&to=2026-09-02T00%3A00%3A00.000Z&asOf=2026-09-02T00%3A00%3A00.000Z";
describe("live analytics route",()=>{it("passes only authorized canonical scope",async()=>{const read=vi.fn(async()=>({}as never));const h=createGetLiveAnalyticsHandler({authorize:async()=>({appUserId:id,allowlistId:id,email:"a@x.invalid",role:"admin"}),repository:{read}});expect((await h(new Request(url,{headers:{authorization:"Bearer x"}}),{id})).status).toBe(200);expect(read).toHaveBeenCalledWith(expect.objectContaining({liveEventId:id,adminAppUserId:id}));});it("rejects malformed IDs and windows",async()=>{const h=createGetLiveAnalyticsHandler({authorize:vi.fn(),repository:{read:vi.fn()}});expect((await h(new Request(url),{id:"bad"})).status).toBe(404);expect((await h(new Request(url.replace("2026-09-01","2026-09-03")),{id})).status).toBe(400);});});

