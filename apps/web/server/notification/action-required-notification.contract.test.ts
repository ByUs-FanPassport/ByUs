import{readFileSync}from"node:fs";import{resolve}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(resolve(process.cwd(),"../../supabase/migrations/20260903030300_phase5_action_required_notifications.sql"),"utf8");
describe("action-required intents",()=>{it.each(["benefit_won","recipient_information_required","fulfillment_meaningful_update","collectible_claim_available","collectible_claim_expiring"])("wires %s at an authoritative DB boundary",kind=>expect(sql).toContain(kind));it("converges by owner/source and never embeds recipient PII",()=>{expect(sql).toContain("on conflict(app_user_id,source_key)do");expect(sql).not.toMatch(/recipient_(?:name|phone|address|postal)/i);});});
