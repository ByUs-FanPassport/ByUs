import{createClient,type SupabaseClient}from"@supabase/supabase-js";
import{createHash}from"node:crypto";
import type{ExternalNotificationJob}from"../external-notification-domain.js";
import type{ExternalNotificationQueue}from"../external-notification-ports.js";
type Rpc=Pick<SupabaseClient,"rpc">;
function job(v:Record<string,unknown>):ExternalNotificationJob{return{id:String(v.id),notificationId:String(v.notification_id),planId:String(v.plan_id),channel:String(v.channel)as ExternalNotificationJob["channel"],sequence:Number(v.sequence)as 1|2,templateKey:String(v.template_key),locale:String(v.locale)as"ko"|"en",destination:String(v.destination),payload:v.payload as ExternalNotificationJob["payload"],attemptCount:Number(v.attempt_count),leaseOwner:String(v.lease_owner),leaseExpiresAt:String(v.lease_expires_at)};}
function redact(value:string){if(value.includes("@")){const[,domain]=value.split("@");return`***@${domain}`;}return`***${value.slice(-4)}`;}
export class SupabaseExternalNotificationQueue implements ExternalNotificationQueue{constructor(private readonly client:Rpc,private readonly environment:"dev"|"prod"){}static create(url:string,key:string,environment:"dev"|"prod"){return new this(createClient(url,key,{auth:{persistSession:false,autoRefreshToken:false,detectSessionInUrl:false}}),environment);}async claim(workerId:string,batchSize:number,leaseSeconds:number){const{data,error}=await this.client.rpc("claim_external_notification_deliveries",{p_worker_id:workerId,p_batch_size:batchSize,p_lease_seconds:leaseSeconds});if(error)throw new Error("external notification claim failed");return((data??[])as Record<string,unknown>[]).map(job);}async complete(value:ExternalNotificationJob,providerMessageId:string){const{data,error}=await this.client.rpc("complete_external_notification_delivery",{p_delivery_id:value.id,p_worker_id:value.leaseOwner,p_provider_message_id:providerMessageId});if(error||data!==true)throw new Error("external notification lease is stale");}async fail(value:ExternalNotificationJob,failure:{code:string;retryable:boolean}){const{data,error}=await this.client.rpc("fail_external_notification_delivery",{p_delivery_id:value.id,p_worker_id:value.leaseOwner,p_error_code:failure.code,p_retryable:failure.retryable});if(error||data!==true)throw new Error("external notification lease is stale");}async recordSink(value:ExternalNotificationJob,result:"sent"|"permanent_failure"|"retryable_failure"){const payloadHash=createHash("sha256").update(JSON.stringify(value.payload)).digest("hex");const{error}=await this.client.rpc("record_notification_test_sink",{p_delivery_id:value.id,p_environment:this.environment,p_channel:value.channel,p_template_key:value.templateKey,p_redacted_destination:redact(value.destination),p_payload_hash:payloadHash,p_result:result});if(error)throw new Error("notification test sink write failed");}}
